@model AFG_New_passport_Website.Models.LocationViewModel

@{
    Layout = "_DashboardLayout";
}
<link href="~/css/dashboardliststyle/dashboardliststyle.css" rel="stylesheet" />
<div class="form-container">
    <div class="card">
<h2>ویرایش لوکیشن</h2>

<form asp-action="Edit" method="post">
    <div class="form-group">
        <label asp-for="Location.NameLocal" class="control-label"></label>
                <input asp-for="Location.NameLocal" class="form-control" />
                <span asp-validation-for="Location.NameLocal" class="text-danger"></span>
    </div>

    <div class="form-group">
                <label asp-for="Location.Name" class="control-label"></label>
                <input asp-for="Location.Name" class="form-control" />
                <span asp-validation-for="Location.Name" class="text-danger"></span>
    </div>

  @*   <div class="form-group">
                <label asp-for="Location.CountryId" class="control-label"></label>
                <select asp-for="Location.CountryId" class="form-control"
                asp-items="@(new SelectList(Model.Countries, "Id", "DName"))">
            <option value="">-- لطفا یک کشور انتخاب کنید --</option>
        </select>
                <span asp-validation-for="Location.CountryId" class="text-danger"></span>
    </div> *@

            <input type="hidden" asp-for="Location.Id" />

            <a asp-action="Index" class="btn btn-secondary"><i class="fas fa-arrow-right me-2"></i>باز گشت</a>
            <button type="submit" class="btn btn-success">
                <i class="fas fa-save me-1"></i> ذخیره تغییرات
            </button>
</form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
